<%= form_for @emergency, :url => emergency_path(@emergency), :method => :patch  do |f| %>
   <%= render :partial => "form", :locals => {:f => f} %>

    <%= f.submit "Update", :class =>"btn btn-primary" %>
<% end %>